box: wercker/nodejs
services:
  - wercker/mongodb
build:
    steps:
      - npm-install
      - script:
         name: mocha test
         code: |
          npm test
          cwd: $WERCKER_ROOT
      - script:
         name: echo nodejs information
         code: |
          echo "node version $(node -v) running"
          echo "npm version $(npm -v) running"